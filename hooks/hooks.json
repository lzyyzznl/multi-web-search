{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "export PATH=\"${CLAUDE_PLUGIN_ROOT:-$PLUGIN_ROOT}/bin:$PATH\""
          }
        ]
      }
    ]
  }
}
